/**
 * components/layout/Divider.tsx
 * ----------------------------------------------------------------------------
 * A thin re-export of ui/separator.tsx under the name section authors will
 * actually look for ("Divider" is the layout-level concept the brief asks
 * for; "Separator" is the Radix primitive it's built on) — no separate
 * implementation to keep in sync with the primitive.
 */
export { Separator as Divider } from "@/components/ui/separator";
