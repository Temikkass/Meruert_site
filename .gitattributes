# Normalize line endings in the repo; check out native on each platform.
# Without this, a Windows clone rewrites every file to CRLF and the first
# commit from a mac/Linux machine shows every line as changed.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
